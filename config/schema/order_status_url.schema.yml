order_status_url.settings:
  type: config_object
  label: 'Order Status URL settings'
  mapping:
    path:
      type: string
      label: 'URL path segment'
    captcha_enabled:
      type: boolean
      label: 'Require CAPTCHA on the verification form'
